# ============================================================================
# VolGuard V5 — Intelligence Edition
# Environment Configuration
# Copy this file to .env and fill in your values.
# Never commit .env to version control.
# ============================================================================


# ── Upstox API (REQUIRED) ────────────────────────────────────────────────────
# Get these from Upstox Developer Portal: https://developer.upstox.com

UPSTOX_ACCESS_TOKEN=your_access_token_here
UPSTOX_CLIENT_ID=your_client_id_here
UPSTOX_CLIENT_SECRET=your_client_secret_here
UPSTOX_REDIRECT_URI=http://localhost:8000/callback
UPSTOX_REFRESH_CODE=your_initial_auth_code_here

# ── LLM Provider — pick ONE (V5 Intelligence Layer) ─────────────────────────
#
# OPTION A: Groq (FREE — recommended for testing)
#   Sign up at: https://console.groq.com
#   Model used: llama-3.3-70b-versatile (free tier, ~30 req/min)
#   Cost: $0
#
GROQ_API_KEY=

#
# OPTION B: Claude / Anthropic (production — $3-6/month for this usage)
#   Sign up at: https://console.anthropic.com
#   Model used: claude-sonnet-4-6
#   Cost: ~$3-6/month
#
ANTHROPIC_API_KEY=

# Priority: if GROQ_API_KEY is set → uses Groq.
#           if only ANTHROPIC_API_KEY is set → uses Claude.
#           if neither is set → V5 intelligence disabled, V4 runs normally.
# To switch from Groq to Claude: unset GROQ_API_KEY, set ANTHROPIC_API_KEY. Done.


# ── Trading Mode ─────────────────────────────────────────────────────────────
# These seed the database defaults on first run.
# After first run, use the /api/system/config endpoint to change live.
#
# WARNING: Set AUTO_TRADING=true only after extensive paper trading.
# Start with: MOCK=true, AUTO=false → validate → MOCK=false, AUTO=false
#             → manual review → AUTO=true

INITIAL_AUTO_TRADING=false
INITIAL_MOCK_TRADING=true


# ── Telegram Alerts (Optional but strongly recommended) ──────────────────────
# Create a bot via @BotFather, get chat ID via @userinfobot
# Leave blank to disable alerts (system still works, silently)

TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=


# ── Database ─────────────────────────────────────────────────────────────────
# SQLite default — good for single-server deployment
# Data is persisted in Docker volume: volguard_data

DATABASE_URL=sqlite:////app/data/volguard.db


# ── Server ───────────────────────────────────────────────────────────────────

PORT=8000
HOST=0.0.0.0
LOG_FILE=/app/logs/volguard.log
